# Backend Service
apiVersion: v1
kind: Service
metadata:
  name: qrgenix-backend
  namespace: qrgenix
  labels:
    app.kubernetes.io/managed-by: qrgenix
spec:
  selector:
    app: qrgenix-backend
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
